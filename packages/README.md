# Package Management & Dependencies Governance

## Overview
This directory contains dependency analysis, audit reports, and license manifests for the PragatiX Web Frontend application.

> Note: Per npm standard, `package.json` and `package-lock.json` remain at the project root.

## Structure
- `dependencies/`: Audit manifests and dependency tree summaries.
- `lockfiles/`: Dependency freeze specifications and audit benchmarks.
